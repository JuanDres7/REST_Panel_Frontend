# Entornos desplegados:
# test       -> https://api-test.restapp.site
# production -> https://api.restapp.site
#
# Para ejecutar tambien el backend en el equipo local, crea un archivo
# .env.test.local (no versionado) con VITE_API_URL=http://localhost:3000.
VITE_API_URL=https://api-test.restapp.site
