services:
  admin:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:?VITE_API_URL is required}
    restart: unless-stopped
    ports:
      - "${ADMIN_HOST_PORT:-8080}:80"
