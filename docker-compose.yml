services:
  admin:
    build:
      context: .
      args:
        APP_ENV: ${APP_ENV:-test}
        VITE_API_URL: ${VITE_API_URL:-https://api-test.restapp.site}
    restart: unless-stopped
    ports:
      - "${ADMIN_HOST_PORT:-8080}:80"
